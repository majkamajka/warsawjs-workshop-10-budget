import React from 'react';
const HelloStateless = ({name}) => <div>Hello stateless {name}</div>; // mogloby byc (props), ale mozemy zdefiniowac, ze z propsow chcemy tylko name => this.props.name

export default HelloStateless;
